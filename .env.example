# Database Configuration - Choose ONE option:

# Option 1: Firebase (Recommended - Easy Setup)
FIREBASE_PROJECT_ID=your-firebase-project-id
USE_FIREBASE=true

# Option 2: PostgreSQL (Advanced Setup)
# DATABASE_URL="postgresql://username:password@localhost:5432/storymagic"
# PGHOST=localhost
# PGPORT=5432
# PGUSER=postgres
# PGPASSWORD=your_password
# PGDATABASE=storymagic

# AI Configuration - Ollama+Mistral (Local AI)
# No API key needed - runs locally with Ollama
# Make sure you have Ollama installed and Mistral model downloaded

# Application Settings
NODE_ENV=development
PORT=5000